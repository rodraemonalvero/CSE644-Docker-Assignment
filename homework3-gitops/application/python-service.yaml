apiVersion: v1
kind: Service

metadata:
  name: python-web-service
  namespace: cse644
  labels:
    app: python-web

spec:
  type: ClusterIP

  selector:
    app: python-web

  ports:
    - name: http
      protocol: TCP
      port: 8888
      targetPort: 8888