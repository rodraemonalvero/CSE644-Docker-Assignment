apiVersion: v1
kind: PersistentVolumeClaim

metadata:
  name: nginx-storage
  namespace: cse644

spec:
  accessModes:
    - ReadWriteOnce

  resources:
    requests:
      storage: 1Gi

  storageClassName: standard